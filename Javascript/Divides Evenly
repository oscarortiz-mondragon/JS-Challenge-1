function dividesEvenly(a, b) {
  if (a % b === 0) return true;
}

----------------------------------------
function dividesEvenly(a, b) {
  let product = a / b;
  let math = JSON.stringify(product);
  let words = math.length;
  if (words <= 2) {
    return true;
  } else {
    return false;
  }
}
